# sil-web

Web site for Social Intelligence Laboratory

## LabWeb の使用方法

### LabWeb の起動

ディレクトリ直下で以下のコマンドで docker を起動
`$ docker-compose up -d`

### LabWeb の停止

ディレクトリ直下で以下のコマンドで docker を停止
`$ docker-compose down`
